# Claude Skills 设计准则

## 第一性原理

设计 skill/command 时，质疑每个步骤的必要性，只保留真正有价值的部分。从用户的核心需求出发，而不是堆砌流程。

## Token 效率

在达到目标的同时尽量节省 token。凡是可以程序化的步骤（文件读写、去重、格式化、规则匹配、排序等），优先用外部脚本（如 Python）实现，LLM 只处理真正需要语义理解的部分。
